# E-Commerce Frontend API Base URL Configuration
VITE_API_BASE_URL=http://localhost:8080/api

# Stripe Checkout frontend feature flag.
# Keep disabled unless the backend Stripe gateway is also enabled with test-mode configuration.
VITE_STRIPE_CHECKOUT_ENABLED=false
